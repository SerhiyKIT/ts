export enum ModalDirection {
	ADD,
	EDIT,
	DEFAULT
};

export enum Option {
	ADD_STATE = 'ADD_STATE',
	EDIT_STATE = 'EDIT_STATE',
	DEFAULT_STATE = 'DEFAULT_STATE'
};

export enum ModalType {
	OPEN_MODAL,
	CLOSED_MODAL
}

export enum Cars {
	ADD,
	EDIT,
	DELETE
}